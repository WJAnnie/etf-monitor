# GS-002

The endpoint fractal windows are disjoint but occupy every processed bar between the endpoint centers, so STRICT_OLD has no independent ProcessedBar. The extreme source raw bars still have three raw bars between them, so RELAXED_LATE accepts the pair.
