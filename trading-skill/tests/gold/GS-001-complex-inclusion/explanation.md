# GS-001

The second and third raw bars are inclusive. Under an UP direction context they merge with high=max(highs) and low=max(lows). The merged bar must preserve both raw-bar identities and summed volume.
